/*====================================================================================================================*/

require([
    'jquery',
    'app'
], function (
    $,
    app
) {
    'use strict';
    window.$.noConflict(true);

    $(app);
});
